# 通用串口协议 V1

本协议用于视觉计算机、主控 MCU、底盘、云台、执行机构和电脑调试工具之间的通信。
以后更换竞赛题目时，保持基础帧结构不变，只新增命令号和对应的有效数据格式。

## 一、数据帧结构

所有多字节数据统一使用小端字节序。

```text
A5 5A | VER | FLAGS | SEQ | SRC | DST | CMD(u16) | LEN(u8)
      | PAYLOAD(LEN字节) | CRC16(u16)
```

| 字段 | 长度 | 说明 |
|---|---:|---|
| `A5 5A` | 2 | 固定双字节帧头 |
| `VER` | 1 | 协议版本，当前固定为 `0x01` |
| `FLAGS` | 1 | ACK、错误和数据有效标志 |
| `SEQ` | 1 | 帧序号，发送后递增，255之后回到0 |
| `SRC` | 1 | 发送设备地址 |
| `DST` | 1 | 接收设备地址 |
| `CMD` | 2 | 命令ID，小端序 |
| `LEN` | 1 | `PAYLOAD`长度，范围为0～240 |
| `PAYLOAD` | N | 当前命令对应的有效数据 |
| `CRC16` | 2 | CRC16校验值，小端序 |

总帧长度为：

```text
12 + LEN
```

CRC使用 `CRC16-CCITT-FALSE`：

```text
多项式：0x1021
初始值：0xFFFF
输出异或：0x0000
计算范围：从VER开始，到PAYLOAD最后一个字节结束
```

帧头 `A5 5A` 和帧尾的CRC值本身不参与CRC计算。

## 二、FLAGS标志位

```text
0x01 NEED_ACK    当前命令需要ACK确认
0x02 IS_ACK      当前帧是ACK回复
0x04 IS_ERROR    当前帧是错误回复
0x08 DATA_VALID  当前业务数据有效
```

高频坐标、巡线误差和实时角度通常不请求ACK。模式切换、启动停止、参数设置、
保存参数等重要命令建议设置 `NEED_ACK`。

## 三、设备地址

```text
0x00 广播地址
0x01 主控MCU
0x02 视觉设备
0x03 云台控制器
0x04 底盘控制器
0x05 执行机构
0xFE 电脑调试端
```

当前23E项目中，Jetson发送数据时使用：

```text
SRC = 0x02
DST = 0x01
```

## 四、当前23E命令

```text
0x0001 心跳
0x0102 A4靶纸（外框+内框）
0x0104 红色激光点
```

### A4靶纸

外框和内框必须来自同一次识别，因此合并在同一帧中发送。
有效数据共36字节：

```text
图像宽度(u16) + 图像高度(u16)
外框：左上、右上、右下、左下，每点x/y均为uint16
内框：左上、右上、右下、左下，每点x/y均为uint16
```

加上固定协议字段后，一帧总长度为48字节。A4识别锁定后立即发送，
之后每2秒低频刷新，重新识别后再发送新坐标。

### 激光点

激光点使用4字节有效数据：

```text
x(u16) + y(u16)
```

加上固定协议字段后，一帧总长度为16字节，约33Hz持续发送。

是否检测到激光点由 `DATA_VALID` 判断，不再使用特殊坐标 `(0, 0)` 表示无效。
没有检测到激光点时，`DATA_VALID=0`，x/y填0。

### 心跳

心跳使用6字节有效数据：

```text
uptime_ms(u32) + state(u8) + error_code(u8)
```

当前视觉程序每500ms发送一次心跳。主控可以在连续多次未收到心跳时判断视觉设备掉线。

## 五、接收流程

接收端不能假设一次串口中断或一次读取就能获得完整数据帧，应使用状态机或缓存解析：

```text
1. 在字节流中寻找 A5 5A
2. 收取固定头部并读取LEN
3. 根据LEN等待完整PAYLOAD和CRC16
4. 校验CRC16
5. 检查DST是否为本机地址或广播地址
6. 根据CMD分发到对应处理函数
7. 如果命令设置NEED_ACK，则返回相同SEQ和CMD的ACK
```

`protocol_v1.py` 中的 `parse_frames()` 已实现噪声过滤、拆包、粘包、
CRC错误丢弃和重新同步。

## 六、代码文件分工

```text
protocol_v1.py       固定协议底层：帧头、序号、地址、CRC、ACK、接收解析
protocol_commands.py 命令号和数据格式表：换题时主要修改这里
red_control.py       23E业务：准备数据、struct.pack、放入串口队列
```

例如激光点格式集中定义为：

```python
CMD_RED_LASER = 0x0104
FMT_RED_LASER = "<HH"
```

业务代码中可以直接看到发送顺序：

```python
payload = struct.pack(
    commands.FMT_RED_LASER,
    x,
    y,
)
```

新增场景时，底层协议不改，只增加 `CMD_XXX`、`FMT_XXX`和对应的
`struct.pack()` / `struct.unpack()`。

## 七、以后如何复用

更换项目时保持以下内容不变：

- 帧头和固定字段
- CRC算法
- 流式解析器
- 设备地址定义
- 命令分类范围
- ACK和心跳机制

新项目只需要：

1. 分配一个新的命令ID。
2. 定义该命令的 `PAYLOAD` 字段。
3. 增加对应的打包和解析函数。
4. 在主控的命令分发函数中增加一个处理分支。

不要直接发送C语言结构体内存，因为不同平台可能存在结构体对齐、类型长度和字节序差异。
每个字段都应按照协议明确打包。

## 八、旧协议兼容

视觉程序默认使用V1协议：

```python
PROTOCOL_MODE = "v1"
```

如果下位机暂时仍使用原来的54字节浮点协议，可切换为：

```python
PROTOCOL_MODE = "legacy"
```

旧协议模式不发送V1心跳。

## 九、测试

运行协议测试：

```bash
python3 -m unittest -v test_protocol_v1.py
```

测试内容包括CRC标准向量、序号循环、矩形和激光点往返解析、拆包、粘包、
CRC错误丢弃以及错误帧后的重新同步。
